model User {
  id    Int     @default(autoincrement()) @id
  email String  @unique
  name  String
  phoneNumber String
  password String
  role String @default("USER")
  isVerified Boolean @default(false)
  refreshToken String?
}